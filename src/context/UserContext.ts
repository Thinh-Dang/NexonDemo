// import { UserContextData } from '@/types';
// import { createContext, useContext, useState } from 'react';
// type Props = {
//   children?: React.ReactNode;
// };
// export const UserContext = createContext<UserContextData | string>(
//   'useContext should be used inside ContextProvider',
// );

// export const UserContextProvider: React.FC<Props> = ({ children }) => {
//   const [userInfo, setUserInfo] = useState<UserContextData>();
//  const verifyGoogle = async()
//   const value = {};
//   return <UserContext.Provider {...{ value, children }} />;
// };

// export const useTasks = () => useContext(UserContext);
